trigger:
  branches:
    include:
      - master
  paths:
    include:
      - src/WebUI/TransportWebUI/*

resources:
- repo: self

variables:
  dockerRegistryServiceConnection: '6df85c80-9463-489d-bf10-2e4ec454e7aa'
  imageRepository: 'transportwebui'
  containerRegistry: 'transportacr.azurecr.io'
  dockerfilePath: 'src/WebUI/TransportWebUI/Dockerfile'
  tag: '$(Build.BuildId)'
  imagePullSecret: 'transportacr1345436e-auth'
  vmImageName: 'ubuntu-latest'

stages:
- stage: Build
  displayName: Build stage
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: $(vmImageName)
    steps:
    - task: Docker@2
      displayName: Build and push an image to container registry
      inputs:
        command: buildAndPush
        repository: $(imageRepository)
        dockerfile: $(dockerfilePath)
        containerRegistry: $(dockerRegistryServiceConnection)
        buildContext: $(Build.SourcesDirectory)/src
        tags: |
          $(tag)

- stage: 'Deploy Staging'
  displayName: Deploy staging
  jobs:
  - job: Deploy
    displayName: Deploy
    steps:
    - task: AzureContainerApps@1
      inputs:
        azureSubscription: 'Azure subscription 1(ac6df0a6-4361-4fcc-b232-1c938d623a03)'
        imageToDeploy: 'transportacr.azurecr.io/transportwebui:$(Build.BuildId)'
        containerAppName: 'transportwebui-container-app'
        resourceGroup: 'transport-microservices-rg'
        containerAppEnvironment: 'transport-microservices-env-prod'
        targetPort: '8080'
        location: 'uksouth'
        ingress: 'external'
        acrName: 'transportacr'

- stage: 'Deploy Production'
  displayName: Deploy production
  jobs:
  - job: Deploy
    displayName: Deploy
    steps:
    - task: AzureContainerApps@1
      inputs:
        azureSubscription: 'Azure subscription 1(ac6df0a6-4361-4fcc-b232-1c938d623a03)'
        imageToDeploy: 'transportacr.azurecr.io/transportwebui:$(Build.BuildId)'
        containerAppName: 'transportwebui-container-app'
        resourceGroup: 'transport-microservices-rg'
        containerAppEnvironment: 'transport-microservices-env-prod'
        targetPort: '8080'
        location: 'uksouth'
        ingress: 'external'
        acrName: 'transportacr'